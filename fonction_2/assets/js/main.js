// Exo 1

// let reverseNumber = (nombre) => {
//     nombre = nombre + ""
//     return nombre.split('').reverse().join('')

// }

// console.log(reverseNumber(31072001));


// Exo 2
/* let estPair = (n) => {
    switch (n % 2) {
        case 0:
            return ("Le nombre " + n + " est pair, il est divisible par 2 : " + n + " / 2   =  " + n / 2)
        case 1:
            return ("Ce nombre est impair, il n'est pas divisible par 2.")
        default:
            alert("salut bg")
    }
}

console.log(estPair(8));
console.log(estPair(200)); */

// Exo 3
/* let motdepasse;
let logIn = () => {
    do {
        motdepasse = prompt("Entrez un mot de passe")
    } while (motdepasse !== "Mufasa");
    alert("Tu es connecté")
}

logIn() */



