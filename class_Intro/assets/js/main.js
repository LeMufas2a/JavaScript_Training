/* // ## EXO1

class Personnage {
    constructor(age, nom, ville) {
        this.age = age
        this.nom = nom
        this.ville = ville
        this.sePresenter = (personne) => {
            console.log(`Bonjour je m'appel ${personne}`);
        }

    }
}

// ## EXO2

let nicky = new Personnage(34, "leto", "anguille-sous-roche")
nicky.sePresenter(nicky.nom)
let leyaske = new Personnage(26, "Akhtar", "Bxl")
leyaske.sePresenter(leyaske.nom) */